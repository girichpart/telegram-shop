{
  "name": "telegram-shop",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "admin",
    "backend",
    "webapp"
  ],
  "scripts": {
    "dev": "npm run dev -w admin & npm run dev -w backend & npm run dev -w webapp",
    "build": "npm run build -w admin && npm run build -w backend && npm run build -w webapp",
    "start": "npm run start -w admin & npm run start -w backend & npm run start -w webapp",
    "lint": "npm run lint -w admin && npm run lint -w backend && npm run lint -w webapp",
    "test": "npm run test -w backend"
  },
  "dependencies": {
    "axios": "^1.7.4",
    "dotenv": "^16.4.5",
    "swiper": "^11.1.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.3.3",
    "typescript": "^5.5.4"
  }
}